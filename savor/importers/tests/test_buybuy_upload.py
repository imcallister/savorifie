import osfrom django.test import TestCasefrom django.db import transactionfrom buybuy import agg_buybuyfrom file_models.buybuy import BuyBuyCSVModelclass BuyBuyUploadTestCase(TestCase):        def test_parse_buybuy_file(self):    	with transaction.atomic():			incoming_name = os.path.join(os.path.dirname(os.path.realpath(__file__)), 'test_files', 'buybuy.csv')			bb_records, errors = BuyBuyCSVModel.import_data(data=open(incoming_name, 'rU'),		                                                    skip_rows=4)			sales_items = agg_buybuy(bb_records)			self.assertEqual(True, False)